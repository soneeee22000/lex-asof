services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: lex
      POSTGRES_PASSWORD: lex
      POSTGRES_DB: lex
    ports: ["5433:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lex"]
      interval: 3s
      timeout: 3s
      retries: 20
